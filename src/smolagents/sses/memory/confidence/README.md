# Confidence Memory Directory

This directory stores confidence assessments generated by the ConfidenceAssessorTool.

Files are stored in JSON format with the naming convention:
`YYYYMMDD_HHMMSS_ID.json`
